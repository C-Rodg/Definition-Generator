import React from 'react';
import NavBar from './Navbar';

const HomePage = () => {
    return (
        <div className="home-page">
            <NavBar />
            Content...
        </div>
    );
};

export default HomePage;